{
  "$schema": "http://json-schema.org/schema#",
  "pluginAlias": "DualCS529",
  "pluginType": "platform",
  "singular": true,
  "headerDisplay": "Dual CS529",
  "footerDisplay": "Dual CS529",
  "schema": {
    "type": "object",
    "properties": {
      "platform": {
        "title": "Platform",
        "type": "string",
        "default": "DualCS529"
      },
      "name": {
        "title": "Platform Name",
        "type": "string",
        "default": "DualCS529"
      },
      "deviceName": {
        "title": "Device Name",
        "type": "string",
        "default": "CS529"
      },
      "peripheralId": {
        "title": "Peripheral ID",
        "description": "BLE MAC/address of the CS529 device.",
        "type": "string"
      },
      "peripheralName": {
        "title": "Peripheral Name",
        "description": "Optional BLE peripheral name to match during scanning.",
        "type": "string"
      },
      "serviceUuid": {
        "title": "Service UUID",
        "description": "BLE control service UUID. Defaults to the Nordic UART service UUID visible in the APK.",
        "type": "string",
        "default": "6e400001-b5a3-f393-e0a9-e50e24dcca9e"
      },
      "commandCharacteristicUuid": {
        "title": "Command Characteristic UUID",
        "description": "BLE characteristic UUID used to write commands. Defaults to Nordic UART RX.",
        "type": "string",
        "default": "6e400002-b5a3-f393-e0a9-e50e24dcca9e"
      },
      "notifyCharacteristicUuid": {
        "title": "Notify Characteristic UUID",
        "description": "BLE characteristic UUID used to receive status notifications. Defaults to Nordic UART TX.",
        "type": "string",
        "default": "6e400003-b5a3-f393-e0a9-e50e24dcca9e"
      },
      "pollIntervalMs": {
        "title": "Poll Interval (ms)",
        "description": "How often to poll current status.",
        "type": "number",
        "default": 120000,
        "minimum": 1000
      },
      "reconnectDelayMs": {
        "title": "Reconnect Delay (ms)",
        "type": "number",
        "default": 2000,
        "minimum": 200
      },
      "reconnectMaxAttempts": {
        "title": "Max Reconnect Attempts",
        "type": "number",
        "default": 15,
        "minimum": 1
      }
    },
    "required": ["platform", "name", "deviceName"]
  }
}
